/* Slave engine: listener vhost / serial transport, request handling
 * from the register image, slave-write events.
 * Filled in by the slave stage. */

#include <libwebsockets.h>
#include "csmb-private.h"

typedef int csmb_slave_placeholder;
